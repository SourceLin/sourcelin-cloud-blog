<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sourcelin.blog.mapper.CommentModerationMapper">

    <insert id="insertCommentModeration" parameterType="com.sourcelin.blog.domain.CommentModeration" useGeneratedKeys="true" keyProperty="id">
        insert into b_comment_moderation (
            comment_id, decision, lexicon_version, trace_id, hit_keywords, create_time
        ) values (
            #{commentId}, #{decision}, #{lexiconVersion}, #{traceId}, #{hitKeywords}, #{createTime}
        )
    </insert>
</mapper>
