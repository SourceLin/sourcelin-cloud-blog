# 网关鉴权单链路策略

## 1. 目标

锁定唯一网关鉴权链路，避免并行实现导致冲突。

## 2. 当前策略声明

- 主链路：`SaTokenGatewayConfiguration`（`SaReactorFilter`）
- 存量实现：`GatewayAuthFilter`（未纳入当前主流程）

## 3. 处置策略

- [x] 明确“仅主链路生效”的架构声明
- [x] 对存量实现标注处置状态：保留但不启用 / 清理计划
- [x] 在发布检查中加入“是否误启用并行过滤器”校验

## 4. 白名单一致性核验

| 路径模式 | 来源配置 | 业务理由 | 预期鉴权行为 | 校验状态 |
|------|------|------|------|------|
| `/auth/login` | `security.ignore.whites` | 登录入口公开 | 放行 | done |

详细核验表：

- `docs/design/2026-04-09-admin-gateway-whitelist-verification.md`

## 5. 验证清单

- [x] 未登录访问公开路径可放行
- [x] 未登录访问受保护路径返回 401
- [x] admin/blog 双域登录态验证通过
- [x] 文件双登录态（upload/confirm）验证通过

## 6. 本轮结论与阻断

- 历史证据目录已从仓库长期文档体系中移除。
- 当前结论保持不变：阻断已解除；脚本已支持 Redis 验证码自动读取，且本地 `sourcelin-blog` 实例注册后，双域登录态与文件双登录态均通过。
